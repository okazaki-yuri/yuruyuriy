# 変更履歴

本プロジェクトの主な変更点を記録する。フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠する。
日付は YYYY-MM-DD（JST）。

## 2026-07-19
### 変更
- 画像を表示サイズに合わせて最適化し、合計サイズを約92%削減（アイコン→128px / ロゴ→480px）。
- 全 `<img>` に `width` / `height` 属性を付与し、外部リンクアイコンに `loading="lazy"` を付与してレイアウトシフト（CLS）を抑制。
- `#header-container` に `min-height` を設定し、ヘッダー・フッターの遅延読み込み時のレイアウトずれを防止。
- YouTubeアイコンの `src` を絶対パスに統一。

### 追加
- 設計ドキュメント一式を追加（`README.md` および `docs/` 配下の各ページ設計書、新規ツール追加手順、デプロイ手順、スタイルガイド、本変更履歴）。
- GitHub の Pull Request / Issue テンプレートを追加（`.github/`）。
- `.gitignore` を追加し、画像原本のバックアップディレクトリを管理対象外に。

## 2026-07-18
### 修正
- HTML構造・入力バリデーション・OGP 等の不具合を修正。

## 2025-09-15
### 変更
- ブログへの遷移導線を調整。
- デプロイ用ワークフロー（`deploy.yaml`）を更新。

## 2025-09-11
### 追加
- `sitemap.xml` を追加（SEO）。
- `robots.txt` を追加（SEO）。

## 2025-09-09
### 修正
- お問い合わせ用メールアドレスを修正。

## 2025-09-07
### 変更
- 独自ドメイン（`tools.yl-yuriy.com`）への変更に伴う対応。
- デプロイ用ワークフローを更新。
